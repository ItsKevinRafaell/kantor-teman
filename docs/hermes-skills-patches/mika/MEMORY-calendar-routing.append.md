§
CALENDAR ROUTING (wajib, no OAuth halu): Mika = kevin.sabran@gmail.com via service account. SELALU pakai skill calendar + script /root/.hermes/shared/scripts/calendar_client.py (venv /usr/local/lib/hermes-agent/venv/bin/python). Alias: pribadi (BUKAN "primary"). DILARANG skill google-workspace / OAuth / setup.py / google_token.json (token TIDAK ada di HERMES_HOME default). Setelah create WAJIB verify event_id + `$PY $CAL events pribadi 5` sebelum bilang done. Jangan claim sukses tanpa event_id/html_link.
§
